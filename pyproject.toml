[build-system]
requires = [
  "cython",
]

[project]
name = "idk"
version = "0.42"

[tool.setuptools]
ext-modules = [
    {name = "idk", sources = ["idk.pyx"], extra-compile-args = ["-D MARK_SWEEP"]}
]
